<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Gold invoice layout: same as standard (A5 via report.paperformat, header hidden via CSS in report body).
        Used by report_invoice_document_gold via t-call override. No xpath here: layout structure
        varies by Odoo version; header hiding and body font are in report_invoice_gold.xml style block.
    -->
    <template id="external_layout_gold" inherit_id="web.external_layout_standard" name="Gold external layout (no header, A5)">
    </template>
</odoo>
