# Gold Pricing Engine - Configuration Example
# ============================================
# 
# This file documents the configuration parameters for the Gold Pricing Engine module.
# These values should be set in Odoo System Parameters, not as environment variables.
#
# To configure in Odoo:
# Settings → Technical → Parameters → System Parameters
#
# Or via Odoo shell:
# self.env['ir.config_parameter'].sudo().set_param('gold_api_endpoint', 'YOUR_VALUE')
# self.env['ir.config_parameter'].sudo().set_param('gold_api_cookie', 'YOUR_VALUE')

# ============================================
# REQUIRED: API Configuration
# ============================================

# Gold Price API Endpoint URL
# This is the URL where the module will fetch gold prices from
GOLD_API_ENDPOINT=https://api.example.com/gold/price

# API Cookie for Authentication
# Cookie value required to authenticate with the API
# Example: session_id=abc123; auth_token=xyz789
GOLD_API_COOKIE=your-cookie-value-here
